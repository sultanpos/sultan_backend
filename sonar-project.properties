sonar.projectKey=sultanpos_sultan_backend
sonar.organization=sultan-pos-server

# Project name and version
sonar.projectName=Sultan Backend
sonar.projectVersion=1.0

# Source directories (include both sultan and sultan_web)
sonar.sources=sultan/src,sultan_web/src

# Test directories to analyze for coverage
sonar.tests=sultan_web/tests

# Exclude test files from main analysis
sonar.exclusions=**/tests/**,**/*_test.rs,**/target/**,**/node_modules/**

# Test file patterns
sonar.test.inclusions=**/tests/**/*.rs,**/*_test.rs

# Encoding of the source code
sonar.sourceEncoding=UTF-8

# Language
sonar.language=rust

# Coverage exclusions (don't require coverage for tests)
sonar.coverage.exclusions=**/tests/**,**/*_test.rs,**/main.rs

# Pull request decoration (for GitHub)
sonar.pullrequest.provider=github
sonar.pullrequest.github.repository=sultanpos/sultan_backend